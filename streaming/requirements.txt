# PySpark version must match the Kafka and Delta JARs in spark_utils.py.
# If you bump pyspark, also update _DELTA_PACKAGE and _KAFKA_PACKAGE in spark_utils.py.
pyspark==3.5.1
delta-spark==3.1.0

# boto3 for EventBridge publishing from the gold job.
# Pinned to a minor version range — boto3 releases frequently but rarely breaks.
boto3>=1.34,<2.0
