# PySpark version must match the Kafka and Delta JARs in spark_utils.py.
# If you bump pyspark, also update _DELTA_PACKAGE and _KAFKA_PACKAGE in spark_utils.py.
pyspark==3.5.1
delta-spark==3.1.0

# confluent-kafka for publishing fraud alerts to the fraud-alerts topic from the gold job.
confluent-kafka>=2.4,<3.0
